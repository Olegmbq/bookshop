# 🧠 Dev Guide — Oleg & Neuro Code Studio

### 📚 Bookshop & 💎 Brand Kit

_(Внутренняя структура и команды разработчика)_

---

## ⚙️ Общая структура проекта

```
bookshop/
├── src/                     # Основной код интернет-магазина
│   ├── assets/              # Изображения, стили, иконки
│   ├── modules/             # JS-модули для логики магазина
│   ├── templates/           # Pug-шаблоны (index, cart и др.)
│   ├── index.js             # Главная точка входа магазина
│   └── assets/styles/       # SCSS / CSS файлы
│
├── brand/                   # Отдельный модуль визитки (Brand Kit)
│   ├── assets/              # Логотип, иконки, стили бренда
│   ├── index.html           # Главная HTML-страница бренда
│   └── index.js             # JS для анимации логотипа и кнопок
│
├── dist/                    # Папка готовой сборки (создаётся автоматически)
├── webpack.config.js        # Сборка магазина (Bookshop)
├── webpack.brand.config.js  # Сборка бренда (визитка)
└── package.json             # Скрипты и зависимости
```

---

## 🚀 Основные команды

| Команда                | Назначение                     | Режим       |
| ---------------------- | ------------------------------ | ----------- |
| `npm run start`        | Запуск магазина Bookshop       | development |
| `npm run build`        | Сборка магазина для продакшена | production  |
| `npm run brand`        | Запуск визитки Brand Kit       | development |
| `npm run clean`        | Очистка папки `dist`           | —           |
| `npm run vercel-build` | Сборка для Vercel              | production  |

---

## 🧩 Webpack конфигурации

### `webpack.config.js` — **Bookshop**

- 📍 Точка входа: `./src/index.js`
- 📄 Шаблоны:

  - `src/templates/index.pug` → `/index.html`
  - `src/templates/cart.pug` → `/cart/index.html`

- 📦 Плагины:

  - HtmlWebpackPlugin
  - MiniCssExtractPlugin
  - CopyWebpackPlugin
  - CleanWebpackPlugin

- ⚙️ Оптимизация: `TerserPlugin`
- 🌍 DevServer: `http://localhost:8080`

---

### `webpack.brand.config.js` — **Brand Kit**

- 📍 Точка входа: `./brand/index.js`
- 📄 Шаблон: `brand/index.html`
- 🎨 Особенности:

  - Собственная папка `dist/brand`
  - Отдельный порт (`8085`, подбирается автоматически)
  - Новый Sass API: `api: "modern-compiler"`
  - Цветной баннер при запуске 💎

---

## 🪶 Что мы с Нейро улучшили

✅ Разделили сборки Bookshop и Brand — теперь они не мешают друг другу
✅ Сохранили полную структуру и оригинальные файлы
✅ Исправили конфликты путей в Webpack
✅ Сохранили все визуальные эффекты и стили
✅ Минимизировали изменения (только в конфиге, без потери логики)
✅ Настроили работу с Vercel для обоих проектов

---

## 💡 Подсказки

- Папка `dist/` создаётся автоматически после сборки
- Если что-то ломается, всегда можно запустить:

  ```bash
  npm run clean
  npm install
  npm run build
  ```

- Для разработки бренда:

  ```bash
  npm run brand
  ```

---

## ✨ Подпись

<p align="center">
  <img src="./brand/assets/logo_neon_holo.png" width="140" alt="Oleg & Neuro Code Studio" /><br><br>
  <b>Oleg & Neuro Code Studio</b><br>
  <i>Создаём в неоне, логике и гармонии 🌌</i><br>
  <sub>© 2025 — Все права света защищены 💜</sub>
</p>
// trigger redeploy
